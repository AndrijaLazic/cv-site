# HTEC Long Cover Test

This is a test post used to preview how the long HTEC cover image renders across:

- Blog carousel and cards
- Blog post hero/header image
- Mobile and desktop viewport sizes

If needed, update `coverImage.position` in this post metadata to fine-tune focus.
